#include <bits/stdc++.h>
using namespace std;

int operationOne(vector<int>& a){
    for(int i = 0 ; i < a.size() ; i++){
        if(!__builtin)
    }
}

int main() {
    int t;
    cin >> t;

    while(t--){
        int n,k;
        cin >> n >> k;

        vector<int>a(n);

        for(int i = 0 ; i < n ; i++) cin >> a[i];


    }


    return 0;
}